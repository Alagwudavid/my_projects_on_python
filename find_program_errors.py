# Type and execute this short program in your editor.
# When it does not work, identify and correct the erroneous statements.
import math
x = math.pi/4
one_val = (math.sin(x)**2) + (math.cos(x)**2)
print(one_val)